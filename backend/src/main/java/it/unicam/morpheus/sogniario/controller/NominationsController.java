package it.unicam.morpheus.sogniario.controller;

import it.unicam.morpheus.sogniario.model.Nomination;

/**
 * The interface extends {@link EntityController} and adds operations to better manage instances of the {@link Nomination} class.
 */
public interface NominationsController extends EntityController<Nomination, String> {
}
