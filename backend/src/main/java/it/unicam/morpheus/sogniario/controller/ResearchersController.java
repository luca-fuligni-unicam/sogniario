package it.unicam.morpheus.sogniario.controller;

import it.unicam.morpheus.sogniario.model.Researcher;

/**
 * The interface extends {@link EntityController} and adds operations to better manage instances of the {@link Researcher} class.
 */
public interface ResearchersController extends EntityController<Researcher, String> {
}
